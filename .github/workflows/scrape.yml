name: DDR Auto Update

on:
  workflow_dispatch: # 手動ボタンで実行可能
  schedule:
    - cron: '0 21 * * *' # 日本時間 朝6時に自動実行 (UTC 21:00)

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: リポジトリを取得
        uses: actions/checkout@v3

      - name: Python環境設定
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: ライブラリインストール
        run: |
          pip install pandas selenium beautifulsoup4 webdriver-manager

      - name: スクレイピング実行
        env:
          DDR_COOKIES: ${{ secrets.DDR_COOKIES }} # Secretを渡す
        run: python cloud_update.py

      - name: 更新データを保存 (Commit & Push)
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add *.csv
          # 変更がある時だけコミットする
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Update data" && git push)